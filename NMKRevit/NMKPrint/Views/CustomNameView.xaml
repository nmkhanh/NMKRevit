<UserControl
  x:Class="NMKRevit.NMKPrint.Views.CustomNameView"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:icon="clr-namespace:Material.Icons.WPF;assembly=Material.Icons.WPF"
  xmlns:model="clr-namespace:NMKRevit.NMKPrint.Models">
  <UserControl.Resources>
    <SolidColorBrush x:Key="CnvPrimaryBrush" Color="#2FD25F" />
    <SolidColorBrush x:Key="CnvPrimaryHoverBrush" Color="#31C85D" />
    <SolidColorBrush x:Key="CnvBorderBrush" Color="#D4D8E1" />
    <SolidColorBrush x:Key="CnvTextBrush" Color="#111827" />

    <Style x:Key="CnvActionButtonStyle" TargetType="Button">
      <Setter Property="Background" Value="{StaticResource CnvPrimaryBrush}" />
      <Setter Property="Foreground" Value="White" />
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="MinHeight" Value="30" />
      <Setter Property="Padding" Value="10,0" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border Background="{TemplateBinding Background}" CornerRadius="6">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource CnvPrimaryHoverBrush}" />
              </Trigger>
              <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Background" Value="#E6E7EC" />
                <Setter Property="Foreground" Value="#9CA3AF" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="CnvIconButtonStyle" TargetType="Button">
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="Foreground" Value="{StaticResource CnvTextBrush}" />
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="MinHeight" Value="30" />
      <Setter Property="Padding" Value="0" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border Background="{TemplateBinding Background}" CornerRadius="4">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#EEF2F7" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="CnvFormatButtonStyle" TargetType="Button">
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="Foreground" Value="{StaticResource CnvTextBrush}" />
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="Height" Value="62" />
      <Setter Property="Padding" Value="0" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border Background="{TemplateBinding Background}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#EEF2F7" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="PdfFormatButtonStyle" TargetType="Button" BasedOn="{StaticResource CnvFormatButtonStyle}">
      <Style.Triggers>
        <DataTrigger Binding="{Binding ActiveFormat}" Value="{x:Static model:PrintFormat.PDF}">
          <Setter Property="Background" Value="#E8F8EE" />
          <Setter Property="Foreground" Value="#138A3D" />
        </DataTrigger>
      </Style.Triggers>
    </Style>

    <Style x:Key="DwgFormatButtonStyle" TargetType="Button" BasedOn="{StaticResource CnvFormatButtonStyle}">
      <Style.Triggers>
        <DataTrigger Binding="{Binding ActiveFormat}" Value="{x:Static model:PrintFormat.DWG}">
          <Setter Property="Background" Value="#E8F8EE" />
          <Setter Property="Foreground" Value="#138A3D" />
        </DataTrigger>
      </Style.Triggers>
    </Style>

    <Style x:Key="CnvRefreshButtonStyle" TargetType="Button" BasedOn="{StaticResource CnvActionButtonStyle}">
      <Setter Property="Width" Value="30" />
      <Setter Property="Height" Value="30" />
      <Setter Property="Padding" Value="0" />
    </Style>

    <Style x:Key="CnvListBoxItemStyle" TargetType="ListBoxItem">
      <Setter Property="Height" Value="30" />
      <Setter Property="VerticalContentAlignment" Value="Center" />
      <Setter Property="HorizontalContentAlignment" Value="Stretch" />
      <Setter Property="Padding" Value="0" />
    </Style>

    <Style x:Key="CnvCenterTextBlockStyle" TargetType="TextBlock">
      <Setter Property="VerticalAlignment" Value="Center" />
      <Setter Property="TextAlignment" Value="Center" />
    </Style>

    <Style x:Key="CnvLeftTextBlockStyle" TargetType="TextBlock">
      <Setter Property="VerticalAlignment" Value="Center" />
      <Setter Property="TextAlignment" Value="Left" />
    </Style>

    <Style x:Key="CnvGridCellStyle" TargetType="DataGridCell">
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="VerticalContentAlignment" Value="Center" />
      <Setter Property="HorizontalContentAlignment" Value="Center" />
      <Style.Triggers>
        <Trigger Property="IsSelected" Value="True">
          <Setter Property="Background" Value="Transparent" />
          <Setter Property="Foreground" Value="{StaticResource CnvTextBrush}" />
        </Trigger>
      </Style.Triggers>
    </Style>

    <Style x:Key="CnvGridTextBoxStyle" TargetType="TextBox">
      <Setter Property="Height" Value="30" />
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="Padding" Value="0" />
      <Setter Property="VerticalContentAlignment" Value="Center" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="TextBox">
            <ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="Center" />
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="CnvGridRowStyle" TargetType="DataGridRow">
      <Setter Property="Height" Value="30" />
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="VerticalContentAlignment" Value="Center" />
    </Style>
  </UserControl.Resources>
  <Border Background="#FFFFFF" BorderBrush="#C9D0DB" BorderThickness="1" CornerRadius="6" Padding="10">
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="34" />
        <RowDefinition Height="*" />
        <RowDefinition Height="40" />
      </Grid.RowDefinitions>

      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="34" />
        </Grid.ColumnDefinitions>
        <TextBlock Text="Custom file name" FontWeight="SemiBold" VerticalAlignment="Center" />
        <TextBlock Grid.Column="1" Text="{Binding ParameterStatus}" Margin="18,0,0,0" Foreground="#6B7280" VerticalAlignment="Center" />
        <Button Grid.Column="2" Style="{StaticResource CnvIconButtonStyle}" Command="{Binding CloseCommand}" ToolTip="Close">
          <icon:MaterialIcon Kind="Close" Width="18" Height="18" />
        </Button>
      </Grid>

      <Grid Grid.Row="1">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="54" />
          <ColumnDefinition Width="310" />
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#F2F4F8" BorderBrush="#D4D8E1" BorderThickness="0,0,1,0" Margin="-10,0,10,0">
          <StackPanel Margin="0,0">
            <Button Margin="0,0,0,2" Style="{StaticResource PdfFormatButtonStyle}" Command="{Binding ShowPdfCommand}" ToolTip="PDF">
              <StackPanel>
                <icon:MaterialIcon Kind="FilePdfBox" Width="22" Height="22" HorizontalAlignment="Center" />
                <TextBlock Text="PDF" FontSize="11" HorizontalAlignment="Center" />
              </StackPanel>
            </Button>
            <Button Style="{StaticResource DwgFormatButtonStyle}" Command="{Binding ShowDwgCommand}" ToolTip="DWG">
              <StackPanel>
                <icon:MaterialIcon Kind="FileCad" Width="22" Height="22" HorizontalAlignment="Center" />
                <TextBlock Text="DWG" FontSize="11" HorizontalAlignment="Center" />
              </StackPanel>
            </Button>
          </StackPanel>
        </Border>

        <Grid Grid.Column="1" Margin="0,10,12,0">
          <Grid.RowDefinitions>
            <RowDefinition Height="38" />
            <RowDefinition Height="*" />
          </Grid.RowDefinitions>
          <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
          <Border Grid.Row="1" BorderBrush="#D5DAE0" BorderThickness="1">
            <ListBox
              ItemsSource="{Binding AvailableItems}"
              SelectedItem="{Binding SelectedItem}"
              BorderThickness="0"
              ItemContainerStyle="{StaticResource CnvListBoxItemStyle}">
              <ListBox.ItemTemplate>
                <DataTemplate>
                  <CheckBox
                    Height="30"
                    VerticalAlignment="Center"
                    VerticalContentAlignment="Center"
                    IsChecked="{Binding IsChecked, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    Content="{Binding Name}"
                    Margin="6,0" />
                </DataTemplate>
              </ListBox.ItemTemplate>
            </ListBox>
          </Border>
        </Grid>

        <Grid Grid.Column="2" Margin="0,10,0,0">
          <Grid.RowDefinitions>
            <RowDefinition Height="38" />
            <RowDefinition Height="*" />
          </Grid.RowDefinitions>
          <TextBox Text="{Binding Preview, TargetNullValue=Preview...}" Foreground="#6B7280" />
          <DataGrid
            Grid.Row="1"
            ItemsSource="{Binding CheckedItems}"
            SelectedItem="{Binding SelectedItem}"
            Margin="0,4,0,0"
            RowHeight="30"
            ColumnHeaderHeight="30"
            GridLinesVisibility="Horizontal"
            HorizontalGridLinesBrush="#EEF2F7"
            RowStyle="{StaticResource CnvGridRowStyle}"
            CellStyle="{StaticResource CnvGridCellStyle}">
            <DataGrid.Columns>
              <DataGridTemplateColumn Header="Index" Width="70">
                <DataGridTemplateColumn.CellTemplate>
                  <DataTemplate>
                    <TextBox
                      Style="{StaticResource CnvGridTextBoxStyle}"
                      Text="{Binding Index, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                      TextAlignment="Center" />
                  </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
              </DataGridTemplateColumn>
              <DataGridTemplateColumn Header="" Width="44">
                <DataGridTemplateColumn.CellTemplate>
                  <DataTemplate>
                    <CheckBox
                      Height="30"
                      VerticalAlignment="Center"
                      VerticalContentAlignment="Center"
                      IsChecked="{Binding IsChecked, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                      HorizontalAlignment="Center" />
                  </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
              </DataGridTemplateColumn>
              <DataGridTemplateColumn Header="Prefix" Width="120">
                <DataGridTemplateColumn.CellTemplate>
                  <DataTemplate>
                    <TextBox Style="{StaticResource CnvGridTextBoxStyle}" Text="{Binding Prefix, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" TextAlignment="Center" />
                  </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
              </DataGridTemplateColumn>
              <DataGridTextColumn Header="Parameter Name" Width="*" Binding="{Binding Name}" IsReadOnly="True" ElementStyle="{StaticResource CnvLeftTextBlockStyle}" />
              <DataGridTemplateColumn Header="Suffix" Width="120">
                <DataGridTemplateColumn.CellTemplate>
                  <DataTemplate>
                    <TextBox Style="{StaticResource CnvGridTextBoxStyle}" Text="{Binding Suffix, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" TextAlignment="Center" />
                  </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
              </DataGridTemplateColumn>
              <DataGridTemplateColumn Header="Separator" Width="120">
                <DataGridTemplateColumn.CellTemplate>
                  <DataTemplate>
                    <TextBox Style="{StaticResource CnvGridTextBoxStyle}" Text="{Binding Separator, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" TextAlignment="Center" />
                  </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
              </DataGridTemplateColumn>
            </DataGrid.Columns>
          </DataGrid>
        </Grid>
      </Grid>

      <Grid Grid.Row="2">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="82" />
          <ColumnDefinition Width="198" />
          <ColumnDefinition Width="34" />
          <ColumnDefinition Width="82" />
          <ColumnDefinition Width="82" />
          <ColumnDefinition Width="38" />
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="90" />
        </Grid.ColumnDefinitions>
        <Button Height="30" Style="{StaticResource CnvActionButtonStyle}" Content="Clear" Command="{Binding ClearSelectionCommand}" Margin="0,5,8,0" />
        <ComboBox Grid.Column="1" ItemsSource="{Binding ImportPresets}" SelectedItem="{Binding SelectedImportPreset}" Margin="0,5,8,0" />
        <Button Grid.Column="2" Style="{StaticResource CnvIconButtonStyle}" Command="{Binding RemoveImportCommand}" Margin="0,5,8,0" ToolTip="Remove selected import">
          <icon:MaterialIcon Kind="TrashCanOutline" Width="18" Height="18" />
        </Button>
        <Button Grid.Column="3" Height="30" Style="{StaticResource CnvActionButtonStyle}" Content="Import" Command="{Binding BrowseImportCommand}" Margin="0,5,8,0" ToolTip="Import JSON template" />
        <Button Grid.Column="4" Height="30" Style="{StaticResource CnvActionButtonStyle}" Content="Export" Command="{Binding ExportCommand}" Margin="0,5,8,0" />
        <Button Grid.Column="5" Style="{StaticResource CnvRefreshButtonStyle}" Command="{Binding ReloadCommand}" Margin="0,5,8,0" ToolTip="Reload">
          <icon:MaterialIcon Kind="Sync" Width="18" Height="18" Foreground="White" />
        </Button>
        <Button Grid.Column="7" Height="30" Style="{StaticResource CnvActionButtonStyle}" Content="Apply" Command="{Binding ApplyCommand}" Margin="0,5,0,0" />
      </Grid>
    </Grid>
  </Border>
</UserControl>
